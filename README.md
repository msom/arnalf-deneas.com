# Arnalf & Deneas Webpage

## Installation

Install the dependencies

    brew install imagemagick

Install the environment

    python -m venv venv
    venv/bin/pip install lektor

## Running

Serve locally

    venv/bin/lektor server

## Deploying

Deploy

    venv/bin/lektor build
    venv/bin/lektor deploy
